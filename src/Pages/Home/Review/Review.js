import React from 'react';

const Review = (props) => {
    // const {service, name, review} = comment;
    const {comment, service_id} = props;

   
   
    return (
        <div>
            
        </div>
    );
};

export default Review;